var x;
var y;

console.log(window.innerWidth);

function setup() {
    // put setup code here
    createCanvas(windowWidth, windowHeight);
    x = width/2;
    y = height/2;
}

function draw() {
    // put drawing code here
    rect(x, y, 10, 10);
    x++;
    y++;
}
